<!-- Project rules live in AGENTS.md so every coding agent shares one source.
     Claude Code reads only CLAUDE.md, so this import pulls AGENTS.md into context
     at session start. Add Claude-specific notes below the import if ever needed.
     (This HTML comment is stripped before injection — it costs no context.) -->
@AGENTS.md
