<!-- Project rules live in AGENTS.md so every coding agent shares one source.
     Claude Code reads only CLAUDE.md, so this import pulls AGENTS.md in.
     Add Claude-specific notes below the import if ever needed. -->
@AGENTS.md
